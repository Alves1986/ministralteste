# STRUCTURE.md — Ministral

Generated by gsd-map-codebase · 2026-05-16

## Árvore de diretórios

```
ministral/
├── GEMINI.MD               ← Regras do Antigravity (prioridade máxima)
├── AGENTS.MD               ← Regras cross-tool
├── App.tsx                 ← Entry point React + roteamento (56 KB)
├── index.tsx               ← ReactDOM.render + QueryClient
├── index.css               ← Estilos globais
├── server.ts               ← Express server (dev + prod local)
├── types.ts                ← Tipos globais TypeScript
├── vite.config.ts          ← Config Vite + PWA plugin
├── tailwind.config.js      ← Design tokens Tailwind
├── tsconfig.json           ← TypeScript strict mode
├── vercel.json             ← Deploy config + headers segurança
├── manifest.json           ← PWA manifest
│
├── components/             ← 50 componentes (screens + UI)
│   ├── AvailabilityScreen.tsx    ← ⚠️ Bug: race condition Realtime
│   ├── ScheduleEditorV2.tsx      ← Componente mais complexo (67 KB)
│   ├── DashboardLayout.tsx       ← Layout principal + navegação
│   ├── LoginScreen.tsx           ← Auth UI
│   ├── RankingScreen.tsx         ← GloryCoin
│   └── ...47 outros componentes
│
├── context/
│   └── SessionContext.tsx        ← ⚠️ Bug: ministry-switching lento
│
├── services/
│   ├── aiOrchestrator.ts         ← IA via OpenRouter
│   ├── scheduleServiceV2.ts      ← Lógica de escalas (16 KB)
│   ├── offlineService.ts         ← Cache offline / PWA
│   ├── cifraClubService.ts       ← Integração CifraClub
│   ├── spotifyService.ts         ← Integração Spotify
│   ├── youtubeService.ts         ← Integração YouTube
│   └── supabase/
│       ├── client.ts             ← Singleton Supabase (ÚNICO client permitido)
│       ├── auth.ts               ← Autenticação
│       ├── ministries.ts         ← CRUD ministérios (12 KB)
│       ├── misc.ts               ← Helpers gerais (17 KB)
│       ├── notifications.ts      ← Web Push (21 KB)
│       ├── ranking.ts            ← GloryCoin (12 KB)
│       ├── scales.ts             ← Escalas
│       ├── availability.ts       ← Disponibilidade
│       ├── events.ts             ← Eventos
│       ├── admin.ts              ← Super admin
│       ├── repertoire.ts         ← Repertório
│       └── index.ts              ← Re-exports
│
├── store/
│   └── appStore.ts              ← Zustand store (UI state global)
│
├── context/
│   └── SessionContext.tsx        ← Auth + org + ministry state
│
├── hooks/                        ← Custom React hooks
├── utils/                        ← Utilitários puros
├── domain/                       ← Regras de domínio
├── application/                  ← Casos de uso / application layer
├── lib/                          ← Bibliotecas internas
├── api/                          ← API routes (Express)
│
├── supabase/
│   └── functions/                ← 12 Edge Functions Deno
│       ├── _shared/
│       ├── push-notification/
│       ├── stripe-create-checkout/
│       ├── stripe-customer-portal/
│       ├── stripe-webhook/
│       ├── validate-invite/
│       └── whatsapp-{connect,disconnect,reminders,swap-notify,webhook,status}/
│
├── public/
│   └── branding/                 ← ⚠️ NÃO modificar sem instrução
│
├── tests/                        ← Vitest
├── .planning/                    ← GSD planning docs
│   └── codebase/                 ← Este mapa
└── .agents/
    └── skills/                   ← Skills do superpowers (junctions)
```

## Contagem
- **123 arquivos** TypeScript/TSX/JSON (excluindo node_modules e dist)
- **50 componentes** em `/components`
- **12 serviços** em `/services/supabase`
- **12 Edge Functions** em `/supabase/functions`
