# INTEGRATIONS.md — Ministral

Generated by gsd-map-codebase · 2026-05-16

## Supabase
- **Auth**: email/password + magic link
- **Database**: PostgreSQL com RLS
- **Realtime**: subscriptions em `AvailabilityScreen`, notificações
- **Storage**: imagens de perfil / branding
- **Edge Functions**: 12 functions Deno deployadas

## Stripe
| Function | Rota | Propósito |
|----------|------|-----------|
| `stripe-create-checkout` | POST | Criar sessão de checkout |
| `stripe-customer-portal` | POST | Portal de gestão do cliente |
| `stripe-webhook` | POST | Processar eventos Stripe |

Planos: `trial` / `pro` / `enterprise`

## WhatsApp (Baileys/CodeChat)
| Function | Propósito |
|----------|-----------|
| `whatsapp-connect` | Conectar instância WhatsApp |
| `whatsapp-disconnect` | Desconectar |
| `whatsapp-status` | Status da conexão |
| `whatsapp-reminders` | Envio automático de lembretes de escala |
| `whatsapp-swap-notify` | Notificação de troca de escala |
| `whatsapp-webhook` | Receber mensagens |

## Web Push (VAPID)
- Edge Function: `push-notification`
- Serviço cliente: `services/supabase/notifications.ts` (21 KB)
- Componente: `components/NotificationCenter.tsx`
- Requer VAPID_PUBLIC_KEY + VAPID_PRIVATE_KEY nas env vars

## OpenRouter (IA)
- Orquestrador: `services/aiOrchestrator.ts`
- Modelos configuráveis — não hardcodado
- Features IA:
  - Geração automática de escalas
  - Sugestões de repertório

## Spotify
- `services/spotifyService.ts`
- Busca de músicas para repertório

## YouTube
- `services/youtubeService.ts`
- Detecção de live streams da igreja

## CifraClub
- `services/cifraClubService.ts`
- Busca de cifras/acordes para músicas do repertório
- Componente: `components/ChordViewer.tsx`

## Variáveis de ambiente necessárias
```env
# Supabase
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Stripe
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
VITE_STRIPE_PUBLISHABLE_KEY=

# WhatsApp (CodeChat)
CODECHAT_API_URL=
CODECHAT_API_KEY=

# Web Push (VAPID)
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=

# OpenRouter
OPENROUTER_API_KEY=

# YouTube
VITE_YOUTUBE_API_KEY=
```
