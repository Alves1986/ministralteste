# STACK.md — Ministral Tech Stack

Generated by gsd-map-codebase · 2026-05-16

## Runtime & Framework
- **React 18.3.1** — functional components + hooks (sem class components)
- **TypeScript 5.2.2** — strict mode obrigatório
- **Vite 5.2.0** — bundler + dev server
- **TailwindCSS 3.4.1** — utility-first CSS (sem UI library externa)

## Backend & Auth
- **Supabase JS 2.39.7** — auth, DB (PostgreSQL), Realtime, Storage
- **Supabase Edge Functions** — Deno/TypeScript (runtime serverless)
- Client singleton em: `services/supabase/client.ts`

## State Management
- **TanStack Query 5.28.4** — server state (fetch, cache, invalidation)
- **Zustand 4.5.2** — UI state global (`store/appStore.ts`)
- **React Context** — session/auth (`context/SessionContext.tsx`)

## Payments
- **Stripe** — checkout + webhook + customer portal
  - Edge Functions: `stripe-create-checkout`, `stripe-webhook`, `stripe-customer-portal`

## Notifications
- **Web Push** — Edge Function `push-notification`
- **WhatsApp (Baileys/CodeChat)** — Edge Functions: `whatsapp-connect`, `whatsapp-disconnect`, `whatsapp-reminders`, `whatsapp-swap-notify`, `whatsapp-webhook`, `whatsapp-status`

## AI
- **OpenRouter** — modelos variáveis via `services/aiOrchestrator.ts` (18 KB)
- **aiService.ts** + **aiScheduleService.ts** — wrappers de features específicas

## External Services
- **Spotify** — `services/spotifyService.ts`
- **YouTube** — `services/youtubeService.ts`
- **CifraClub** — `services/cifraClubService.ts` (cifras/acordes)

## Export/Reports
- **jsPDF 2.5.1** + **jspdf-autotable** — geração de PDF para relatórios
- **qrcode 1.5.4** — QR Code para convites

## PWA
- **vite-plugin-pwa 1.2.0** — service worker, manifest, install banner
- `manifest.json` + `components/InstallBanner.tsx` + `components/InstallModal.tsx`

## Server (dev/prod local)
- **Express 5.2.1** via **tsx 4.21.0** — `server.ts`
- Scripts: `npm run dev` / `npm start` → `tsx server.ts`

## Testing
- **Vitest 4.0.18** — test runner
- **@testing-library/react 16.3.2** + **jsdom 28.1.0**
- Pasta: `tests/`

## Deploy
- **Vercel** — frontend + API routes
- `vercel.json` configurado com headers de segurança
- Edge Functions: deploy via Supabase CLI
